
public class WackAMole {
	/**create UI
	 * 	frame
	 * 	panel	
	 * 	generate a random number from 1-20
	 * 	for i= 1....20
	 * 		makeButton()
	 * 		if(i==randomnumber)
	 * 			make text says "mole"
	 *
	 */
	
	/*action perform
	 * 	check pressed button
	 * 	if mole button pressed
	 * 		disposed fram : frame.dispose();
	 * 		createUI()
	 * 	else
	 * 		play loser sound effect
	 * 
	 */
	
	/**makeButton()
	 * 	instantiate button
	 * 	add listener
	 * 	add to panel
	 * 	
	 * 	
	 */
}
